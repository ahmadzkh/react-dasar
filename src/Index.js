import React from "react";
import ReactDOM from "react-dom";
import "./style.css"; // Pastikan CSS diimpor
import App from "./App"; // Mengimpor komponen utama

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
